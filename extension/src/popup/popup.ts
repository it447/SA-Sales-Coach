/**
 * Popup script. Phase 0 stub — Phase 3 will add the "start call" button
 * that creates a session for the active Meet tab.
 */

document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  if (root) {
    root.textContent = "Deal Assistant (scaffold — Phase 3 adds the real UI)";
  }
});
